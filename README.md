# DataEmbeddingtoGrayScaleMedicalImage
Cryptography Term Project.
The code encrypts patient data and then embeds it into a grayscale medical image.
It also reads the data from the image and decrypts it
